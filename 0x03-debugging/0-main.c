#include <stdio>

/**
 * main - Test a positive_or_negative() function
 *
 * @i: parameter passed to the function
 *
 * Return: Always 0 on success
 */
int main(void);
{
	int i;

	i = 0;
	positive_or_negative(i);

	retun (0);
}
