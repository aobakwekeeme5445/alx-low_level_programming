#include "main.h"

/**
 * _abs - Computes absolute value of an integer
 *
 * @int: integer used to return its
 * absolute value
 *
 * Return: O on success
 */
int _abs(int)
{
	if (int < 0)
		_putchar(-(int));
	else
		_putchar(int);

	return (0);
}
