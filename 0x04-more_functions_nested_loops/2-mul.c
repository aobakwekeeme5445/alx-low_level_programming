#include <stdio.h>

/**
 * mul - Multiplies two integers a, b
 *
 * @a: integer1
 * @b: integer2
 *
 * Return: * of a, b
 */
int mul(int a, int b)
{
	int mult;

	mult = (a) * (b);
	printf(mult);

	return (mult);
}
